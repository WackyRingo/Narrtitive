# Everything below is optional and free — the scanner runs with no
# .env file at all. Only fill these in for Telegram push alerts
# instead of console-only (Telegram's bot API doesn't charge for this).
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
