# Everything below is optional and free.

# Telegram push alerts instead of console-only (Telegram's bot API is free).
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Solana Tracker — powers the Security line (rug score, snipers, bundlers,
# insiders) for Solana tokens. Free tier: 2,500 requests/month, no card.
# Sign up at solanatracker.io, grab a key from the dashboard.
SOLANA_TRACKER_API_KEY=
